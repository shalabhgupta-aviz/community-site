export function logError(error) {
  console.error('Logged Error:', error);
  // Here you can integrate with Sentry, LogRocket, or any other logging service
  // Example: Sentry.captureException(error);
} 